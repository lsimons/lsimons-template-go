# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
dist/
# default CLI output from `go build ./...` on *nix (matches module name)
/lsimons-template
# After `mise run init` replaces the placeholder, keep the module-named
# binary out of git by renaming this line to /<your-project>.

# Test binaries and output
*.test
*.out
coverage.out
coverage.html
coverage.txt

# Go workspace (single-module repo by default)
go.work
go.work.sum

# Vendored dependencies (opt-in only)
vendor/

# Go build/test cache
.cache/

# Profiles and traces
*.prof
*.trace

# Environment
.env
.env.*

# IDEs and editors
.vscode/
.idea/
*.swp
*.swo
*~

# Operating systems
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Claude local settings
.claude/settings.local.json
